{
    "version": "2.0.0",
    "options": 
    {
        "env": 
        {
            "sm_path": "/home/dev/GFL/SourceMod/scripting"
        }
    },
    "tasks": 
    [
        {
            "label": "sourcepawn",
            "type": "shell",
            "command": "$sm_path/spcomp64 ${file} -D ${workspaceFolder} -i ${workspaceFolder}/scripting/include && sudo cp -f ${workspaceFolder}/${fileBasenameNoExtension}.smx /home/roy/css/cstrike/addons/sourcemod/plugins",
            "problemMatcher": []
        }
    ]
}